import { TestBed } from '@angular/core/testing';

import { MinibusLinesService } from './minibus-lines.service';

/*describe('MinibusLinesService', () => {
  /*beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: MinibusLinesService = TestBed.get(MinibusLinesService);
    expect(service).toBeTruthy();
  });
});*/
